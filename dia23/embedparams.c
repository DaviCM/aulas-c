#include <stdio.h>

/* existem alguns parâmetros para um #embed, todos bem objetivos:
   limit(), que recebe o limite de valores que podem ser lidos.

   prefix(), que recebe valores para colocar como prepend aos
   bytes lidos. será ignorada se o arquivo estiver vazio.

   suffix(), que recebe valores para colocar como append aos bytes
   lidos. será ignorada se o arquivo estiver vazio.
   
   e um check para verificar se um arquivo pode receber embed:
   __has_embed__(), que recebe filename.
*/

int main(void) {
    
    return 0;
}